-- 050_resend_schedule_inbox.sql — שליחה מחדש מתוזמנת יכולה לצאת ממספר אחר.
--
-- הסיבה השכיחה לכישלון המוני היא ירידת דירוג האיכות של המספר אצל מטא. שליחה חוזרת
-- מאותו מספר מחריפה את הדירוג במקום לתקן, ולכן צריך לבחור מספר אחר — וגם ריצה מתוזמנת
-- חייבת לזכור את הבחירה. NULL = המספר של הקמפיין (ההתנהגות שהייתה עד כה).
ALTER TABLE drip.campaign_resend_schedule
  ADD COLUMN IF NOT EXISTS inbox_id bigint;
